const estudante = require('./estudante.json');
//requier está importando os dados do JSON
//import estudante from './estudantes.json'

//console.log(estudante);
//console.log(typeof estudante);

// Aqui estamos convertendo o json para javascript.
const chave = Object.keys(estudante);
console.log(chaves);